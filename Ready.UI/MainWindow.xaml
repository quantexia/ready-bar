<Window x:Class="Ready.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ready.UI"
        xmlns:controls="clr-namespace:Ready.UI.Controls"
        mc:Ignorable="d" 
        Title="Ready Bar" 
        Height="250" Width="200"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        >
    <DockPanel LastChildFill="True">
        <StackPanel x:Name="spButtons" DockPanel.Dock="Bottom" Orientation="Horizontal">
            <Button Content="Init" Width="50" Click="Button_Click"></Button>
            <Button Content="Add" Width="50" Click="Button_Click" />
            <Button Content="Remove" Width="50" Click="Button_Click" />
            <Button Content="Ex" Width="50" Click="Button_Click" />
        </StackPanel>
        <controls:LaunchableMonitorView />
    </DockPanel>
</Window>
